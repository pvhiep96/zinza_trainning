/ Static navbar
%nav.navbar.navbar-default
  .container
    .navbar-header
      %button.navbar-toggle.collapsed{"aria-controls" => "navbar", "aria-expanded" => "false", "data-target" => "#navbar", "data-toggle" => "collapse", :type => "button"}
        %span.sr-only Toggle navigation
        %span.icon-bar
        %span.icon-bar
        %span.icon-bar
      %a.navbar-brand{:href => "http://localhost:3000/products/showall"} Project name
    #navbar.navbar-collapse.collapse
      %ul.nav.navbar-nav
        %li= link_to t("home"), products_showall_path, class: "active"
        %li.dropdown
          %a.dropdown-toggle{"data-toggle" => "dropdown", href: "#"}
            = current_user.name
            %b.caret
          %ul.dropdown-menu
            %li
              = link_to "Profile", edit_user_registration_path,class: "btn btn-link"
            %li
              = link_to t("list_order"), shop_showorder_path,class: "btn btn-link"
            %li
              = link_to "My Cart", orders_path, class: "btn btn-link"
            %li
              = link_to "My product", products_path, class: "btn btn-link"
            - if user_signed_in?
              %li
                = link_to t("logout"), destroy_user_session_path, method: 'delete',class: "btn btn-link"
        -# %li
          -# - if user_signed_in?
            -# %li= link_to current_user.name, edit_user_registration_path
          -# - else
            -# %li= link_to t("login"), new_user_session_path
        -# %li= link_to t("category"), categories_path
        -# %li= link_to t("product"),  products_path
      = search_form_for @search, class: "navbar-form navbar-left" do |f|
        .form-group
          = f.text_field :name_cont, placeholder: "enter name", class: "form-control"
      %ul.nav.navbar-nav.navbar-right
        -# %li
          -# %div
            -# = link_to t("list_order"), shop_showorder_path,class: "btn btn-link"
        -# %li
          -# %div
            -# = link_to "My Cart", orders_path, class: "btn btn-link"
        %li
          #cart.cart-text
            - if user_signed_in?
              = render 'shared/cart_text', order: @order
        -# %li
          -# %div
            -# - if user_signed_in?
              -# = link_to t("logout"), destroy_user_session_path, method: 'delete',class: "btn btn-link"
        %li.translate
          = link_to "En", locale:"en"
        %li.translate
          = link_to "Vi", locale:"vi"
    / /.nav-collapse
  / /.container-fluid
